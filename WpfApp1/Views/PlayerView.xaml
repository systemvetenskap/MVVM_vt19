<Window x:Class="WpfApp1.Views.PlayerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="PlayerView" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="23" Margin="105,175,0,0" TextWrapping="Wrap" Text="{Binding Path=Firstname, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="120"/>
        <Label x:Name="label" Content="{Binding Path=Firstname}" HorizontalAlignment="Left" Margin="95,76,0,0" VerticalAlignment="Top" RenderTransformOrigin="1.02,0.521" Height="44" Width="60"/>
        <TextBox x:Name="textBox1" HorizontalAlignment="Left" Height="23" Margin="140,262,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <Button x:Name="button" Content="Save" Command="{Binding SaveCommand}" HorizontalAlignment="Left" Margin="396,198,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
